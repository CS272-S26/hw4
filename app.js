
function checkout() {
    const numDrinks = getNumDrinks();
    const pricePerDrink = getPricePerDrink();
    const tipPercent = getTip();
    const hasLoyalty = getLoyaltyStatus();

    // TODO Calculate the total and alert the user of the results!
    alert("I should check that all values have been entered do the calculation!")
}
